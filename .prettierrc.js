module.exports = {
  // String string usa o número de cotação única
  singleQuote: true,
  // Adicione automaticamente um número de segmento no final de cada linha
  semi: false,
  // A guia recua do tamanho, padrão para 2
  tabWidth: 2,
  // Use a guia para recuar, padrão false
  useTabs: false,
  // O espaço de impressão no objeto é verdadeiro
  // true: { foo: bar }
  // false: {foo: bar}
  bracketSpacing: true,
// Parâmetro da função de seta PARELESE PARECHAPLE Evite evitar | sempre
  // Quando evitar, pode omitir parênteses, omitido por exemplo x => x
  // sempre tem parênteses
  arrowParens: 'avoid',
// Altere o comprimento, padrão 80
  printWidth: 80,

  jsxBracketSameLine: true,
}
